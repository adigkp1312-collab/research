# LangChain Core
langchain>=0.3.0
langchain-core>=0.3.0

# Gemini (Direct API - no OpenRouter)
langchain-google-genai>=2.0.0

# Observability (optional)
langsmith>=0.2.0

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Utilities
python-dotenv>=1.0.0  # Only for local dev (Lambda doesn't use .env)
pydantic>=2.0.0  # Required by FastAPI

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
